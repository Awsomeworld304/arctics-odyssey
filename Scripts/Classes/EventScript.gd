extends Node
## The Event Script allows scripting for gameplay events that are triggered via invisible markers in the chart.
class_name EventScript

# Called when the node enters the scene tree for the first time.
func _ready() -> void:
	pass

# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta:float) -> void:
	pass

# Called when the event gets triggered during the game.
func _on_event() -> void:
	pass
